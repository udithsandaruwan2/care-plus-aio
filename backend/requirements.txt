# ── Care Plus backend (Python 3.11) ──────────────────────────────
# Core web
Django==4.2.16
djangorestframework==3.15.2
django-environ==0.11.2
django-cors-headers==4.4.0
whitenoise==6.7.0

# Database (GeoDjango uses PostGIS; psycopg3 binary needs no libpq-dev)
psycopg[binary]==3.2.3

# Realtime + async (wired fully in Step 4)
channels==4.1.0
channels-redis==4.2.1
daphne==4.1.2
uvicorn[standard]==0.32.0
celery==5.4.0
redis==5.2.0

# Auth
djangorestframework-simplejwt==5.3.1

# Cognitive layer (voice → intent). Optional at runtime: the stub extractor
# works without it; installed for the Gemini backend when a key is present.
google-generativeai==0.8.3
# Gemini TTS (speech generation) — used by apps.voice.tts
google-genai==1.14.0

# Matching / CBF (Step 17) — FAISS IndexFlatIP + numpy. Default embedder is
# deterministic hashing; install sentence-transformers separately for e5.
numpy==2.1.3
faiss-cpu==1.9.0.post1

# Personalization / CF (Step 21) — implicit ALS on interaction log.
implicit==0.7.2
scipy==1.14.1

# Local multilingual ASR (Care Plus own framework — not Gemini).
# Downloads Whisper weights on first use into WHISPER_DOWNLOAD_ROOT.
faster-whisper==1.1.1

# Neural TTS fallback for Sinhala/Tamil when Gemini TTS quota is exhausted.
edge-tts==7.2.8

# Medical record field encryption (Step 34)
cryptography>=49.0.0,<50
